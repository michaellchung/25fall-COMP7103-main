# 🚀 TravelMate AI 快速开始指南

## 📋 前置要求

### 必需软件
- **Python 3.9+** 
- **Node.js 16+** 和 npm
- **Git**

### API密钥（必需）
- OpenAI API Key（用于LLM）
- 和风天气 API Key（免费版）
- 高德地图 API Key（可选，用于前端地图）

---

## ⚡ 一键启动（推荐）

### MacOS/Linux

```bash
# 1. 克隆或进入项目目录
cd /Applications/MyDocument/7103

# 2. 配置环境变量
cp env.example .env
# 编辑.env文件，填入你的API密钥

# 3. 运行启动脚本
./scripts/start_dev.sh
```

### Windows

```powershell
# 手动启动，见下方分步说明
```

---

## 📦 分步启动（详细版）

### Step 1: 配置环境变量

```bash
# 复制环境变量模板
cp env.example .env

# 编辑.env文件
vim .env  # 或使用你喜欢的编辑器
```

**必填项：**
```env
OPENAI_API_KEY=sk-your-key-here
WEATHER_API_KEY=your-qweather-key-here
```

### Step 2: 启动后端

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（首次）
python3 -m venv venv

# 激活虚拟环境
# MacOS/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 启动FastAPI服务
python main.py

# 或使用uvicorn（推荐）
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**验证后端：**
- 浏览器访问：http://localhost:8000
- API文档：http://localhost:8000/docs

### Step 3: 启动前端

**新开一个终端窗口：**

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次）
npm install

# 启动开发服务器
npm run dev
```

**访问前端：**
- 浏览器访问：http://localhost:5173

---

## 🧪 验证安装

### 1. 检查后端

```bash
# 健康检查
curl http://localhost:8000/health

# 预期输出：
{
  "status": "healthy",
  "service": "TravelMate AI",
  "version": "1.0.0"
}
```

### 2. 检查前端

浏览器访问 http://localhost:5173，应该看到：
- 标题："TravelMate AI"
- 欢迎消息
- 对话输入框

### 3. 测试对话功能

在前端输入框输入："你好"，点击发送
- 如果收到回复（即使是示例回复），说明前后端连接成功 ✅

---

## 🔧 常见问题

### Q1: 端口被占用

**后端端口8000被占用：**
```bash
# 查找占用进程
lsof -i :8000
# 杀死进程
kill -9 <PID>

# 或修改端口
# 编辑 backend/config/settings.py
API_PORT: int = 8001
```

**前端端口5173被占用：**
```bash
# Vite会自动使用下一个可用端口
# 或修改 frontend/vite.config.js
server: {
  port: 3000
}
```

### Q2: Python虚拟环境问题

```bash
# 删除旧环境
rm -rf backend/venv

# 重新创建
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Q3: npm安装失败

```bash
# 清除缓存
cd frontend
rm -rf node_modules package-lock.json
npm cache clean --force

# 重新安装
npm install

# 如果还失败，尝试使用淘宝镜像
npm install --registry=https://registry.npmmirror.com
```

### Q4: API密钥未配置

错误信息：
```
OpenAI API key not found
```

解决：
```bash
# 检查.env文件是否存在
ls -la .env

# 确保.env文件在项目根目录
# 且包含正确的API密钥
cat .env | grep OPENAI_API_KEY
```

---

## 📂 项目结构概览

```
7103/
├── backend/          # FastAPI后端
│   ├── main.py      # 入口文件
│   └── ...
├── frontend/         # Vue.js前端
│   ├── src/
│   └── ...
├── data/            # 数据文件
│   ├── attractions/ # 景点数据
│   └── ...
└── .env             # 环境变量（需自行创建）
```

---

## 🎯 下一步

项目启动成功后，你可以：

1. **查看项目文档**
   - PRD.md - 产品需求文档
   - 团队分工方案.md - 开发计划
   - README_项目结构.md - 结构说明

2. **开始开发**
   - 后端：实现Agent核心逻辑
   - 前端：完善UI组件
   - 数据：收集景点数据

3. **测试功能**
   - 打开浏览器开发者工具
   - 查看Network标签
   - 测试API调用

---

## 💡 开发技巧

### 热重载

- **后端**：使用 `uvicorn main:app --reload` 自动重启
- **前端**：Vite自动支持热模块替换(HMR)

### 查看日志

**后端日志：**
```bash
# 终端会显示所有请求日志
# 格式：INFO:     127.0.0.1:xxxxx - "GET /api/xxx HTTP/1.1" 200 OK
```

**前端日志：**
```javascript
// 浏览器Console
console.log('调试信息')
```

### API调试

使用Swagger UI：
1. 访问 http://localhost:8000/docs
2. 展开任意接口
3. 点击"Try it out"
4. 填写参数后点击"Execute"

---

## 🆘 获取帮助

遇到问题？

1. **查看文档**：README_项目结构.md
2. **检查日志**：终端输出
3. **联系团队**：微信群/GitHub Issues

---

**祝开发顺利！** 🎉

