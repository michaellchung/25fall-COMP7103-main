# 团队分工方案
## TravelMate AI - 基于FastAPI + Vue.js + RAG架构

---

## 📋 项目概览

**技术架构：**
- **前端**：Vue.js 3 + Element Plus
- **后端**：FastAPI + Python 3.9+
- **核心技术**：LLM Agent + RAG知识库
- **数据库**：ChromaDB（向量） + SQLite（关系型）

**开发周期：** 10天
**团队规模：** 4人

---

## 👥 团队成员与核心职责

| 成员 | 角色 | 核心职责 | 技术栈 |
|------|------|---------|--------|
| 成员A | 项目负责人 + Agent核心开发 | Agent逻辑、LLM集成、提示词工程、整体架构 | Python, LangChain, OpenAI API |
| 成员B | 数据工程师 + RAG系统开发 | 知识库构建、向量检索、数据收集与处理 | Python, ChromaDB, Embeddings |
| 成员C | 后端工程师 + 算法开发 | FastAPI开发、行程规划算法、工具链 | FastAPI, Pydantic, 算法设计 |
| 成员D | 前端工程师 + UI/UX | Vue.js开发、页面设计、用户交互 | Vue.js, Element Plus, CSS |

---

## 🎯 成员A：项目负责人 + Agent核心开发

### 负责模块

#### 1. Agent核心控制器 (`backend/agent/core.py`)
**功能：**
- Agent主控制逻辑
- 工具调用决策与编排
- 状态管理与流程控制
- 与LLM的交互协调

**关键类/方法：**
```python
class AgentCore:
    def __init__(...)
    def process_message(user_input: str) -> AgentResponse
    def decide_tool_calls(state: ConversationState) -> List[ToolCall]
    def generate_itinerary() -> Itinerary
```

#### 2. 对话管理器 (`backend/agent/dialogue.py`)
**功能：**
- 多轮对话上下文管理
- 需求信息提取
- 对话阶段判断（greeting/collecting/generating/modifying）
- 缺失信息追问

**关键类/方法：**
```python
class DialogueManager:
    def process_user_input(user_input: str) -> str
    def extract_requirements(user_input: str) -> UserRequirements
    def check_completeness() -> bool
    def generate_follow_up_question() -> str
```

#### 3. 状态管理 (`backend/agent/state.py`)
**功能：**
- 用户需求数据模型
- 对话历史记录
- 工具调用记录

**关键数据模型：**
```python
class UserRequirements(BaseModel):
    destination: Optional[str]
    days: Optional[int]
    budget: Optional[int]
    preferences: List[str]
    travel_dates: Optional[str]
    companions: Optional[str]

class ConversationState(BaseModel):
    user_requirements: UserRequirements
    dialogue_history: List[Dict]
    current_stage: str
```

#### 4. LLM接口封装 (`backend/utils/llm.py`)
**功能：**
- OpenAI API调用
- 流式输出支持
- 错误处理与重试
- Token计数与成本控制

#### 5. 提示词工程 (`backend/config/prompts.py`)
**功能：**
- 信息抽取提示词
- 对话生成提示词
- 行程优化提示词
- 各场景提示词模板

### 产出物清单

1. **代码模块：**
   - `backend/agent/core.py` - Agent核心控制器
   - `backend/agent/dialogue.py` - 对话管理器
   - `backend/agent/state.py` - 状态管理
   - `backend/utils/llm.py` - LLM封装
   - `backend/config/prompts.py` - 提示词库

2. **文档：**
   - Agent架构设计文档
   - 提示词设计说明
   - LLM使用规范

3. **测试：**
   - Agent核心逻辑单元测试
   - 对话流程端到端测试

### 对接点

**→ 与成员B对接：**
- **接口：** RAG检索接口
- **调用时机：** 用户询问景点信息、生成行程时需要攻略补充
- **数据格式：**
  ```python
  # 调用RAG
  results = rag_retriever.query(
      query="杭州西湖最佳游览路线",
      city="杭州",
      top_k=3
  )
  # 返回：List[Document]
  ```

**→ 与成员C对接：**
- **接口：** 工具链接口（天气、景点、路线、预算）
- **调用时机：** Agent决策需要调用工具时
- **数据格式：**
  ```python
  # 调用工具
  weather = weather_tool.get_forecast(city="杭州", days=3)
  attractions = attraction_tool.search(city="杭州", preferences=["文化"])
  ```

**→ 与成员D对接：**
- **接口：** 通过成员C的API间接对接
- **职责：** 确保API返回数据格式满足前端展示需求
- **Review前端交互逻辑是否合理**

### 工作重点

1. **提示词工程**：信息抽取准确率是关键
2. **流程控制**：对话阶段切换要自然流畅
3. **错误处理**：LLM调用失败时的降级策略
4. **代码质量**：作为项目负责人要保证代码规范

---

## 🗄️ 成员B：数据工程师 + RAG系统开发

### 负责模块

#### 1. RAG向量存储 (`backend/rag/vector_store.py`)
**功能：**
- ChromaDB初始化与配置
- 文档向量化与存储
- Collection管理（按城市/类型分类）
- 数据增删改查

**关键类/方法：**
```python
class VectorStore:
    def __init__(db_path: str)
    def add_documents(documents: List[Document]) -> None
    def delete_collection(name: str) -> None
    def get_collection(name: str) -> Collection
```

#### 2. RAG检索器 (`backend/rag/retriever.py`)
**功能：**
- 语义检索
- 混合检索（语义 + 关键词）
- 结果过滤与排序
- Top-K结果返回

**关键类/方法：**
```python
class RAGRetriever:
    def query(query: str, city: str, top_k: int) -> List[Document]
    def semantic_search(query: str) -> List[Document]
    def hybrid_search(query: str, keywords: List[str]) -> List[Document]
```

#### 3. 嵌入模型封装 (`backend/rag/embeddings.py`)
**功能：**
- OpenAI Embeddings封装
- 文本向量化
- 批量处理优化

#### 4. 数据收集与处理 (`scripts/build_knowledge_base.py`)
**功能：**
- 景点数据爬取/整理
- 攻略文档收集
- 文本清洗与标准化
- 数据分块（Chunking）

#### 5. 天气查询工具 (`backend/tools/weather.py`)
**功能：**
- 和风天气API集成
- 天气数据获取与解析
- 天气建议生成

**关键类/方法：**
```python
class WeatherTool:
    def get_forecast(city: str, days: int) -> List[WeatherData]
    def parse_weather_advice(weather_data: WeatherData) -> str
```

### 产出物清单

1. **代码模块：**
   - `backend/rag/vector_store.py` - 向量存储
   - `backend/rag/retriever.py` - 检索器
   - `backend/rag/embeddings.py` - 嵌入模型
   - `backend/rag/reranker.py` - 重排序（可选）
   - `backend/tools/weather.py` - 天气工具
   - `scripts/build_knowledge_base.py` - 知识库构建脚本

2. **数据文件：**
   - `data/attractions/*.json` - 景点数据（200条）
     - guangdong.json（50条）
     - jiangsu.json（50条）
     - zhejiang.json（100条）
   - `data/knowledge_base/guides/` - 攻略文档（50-70篇）
   - `data/vector_db/chroma/` - 向量数据库文件

3. **文档：**
   - RAG系统设计文档
   - 数据格式说明文档
   - 知识库构建指南

4. **测试：**
   - RAG检索效果测试
   - 数据质量验证

### 对接点

**→ 与成员A对接：**
- **提供接口：** `RAGRetriever.query()` 方法
- **接口规范：**
  ```python
  def query(
      query: str,           # 用户问题或查询关键词
      city: Optional[str],  # 限定城市
      top_k: int = 5       # 返回结果数
  ) -> List[Document]:
      """
      返回格式：
      [
          {
              "content": "西湖最佳游览路线...",
              "metadata": {
                  "source": "马蜂窝",
                  "city": "杭州",
                  "type": "guide"
              },
              "score": 0.85
          },
          ...
      ]
      """
  ```

**→ 与成员C对接：**
- **提供接口：** 景点数据文件路径和格式
- **数据格式：**
  ```json
  {
      "id": "hz001",
      "name": "西湖",
      "city": "杭州",
      "province": "浙江省",
      "category": ["自然风光", "文化"],
      "rating": 4.8,
      "visit_duration": 3,
      "ticket_price": 0,
      "location": {"lat": 30.2591, "lng": 120.1353},
      "description": "...",
      "tags": ["必游", "免费"]
  }
  ```
- **协作内容：** 成员C读取景点数据用于行程规划

**→ 与成员D对接：**
- 间接对接，通过API提供数据

### 工作重点

1. **数据质量**：景点数据和攻略文档的质量直接影响效果
2. **检索效果**：调优embedding模型和检索策略
3. **性能优化**：向量检索速度要快（<100ms）
4. **数据组织**：合理的数据分类和索引结构

---

## ⚙️ 成员C：后端工程师 + 算法开发

### 负责模块

#### 1. FastAPI主程序 (`backend/main.py`)
**功能：**
- FastAPI应用初始化
- 中间件配置（CORS、日志）
- 路由注册
- 生命周期管理

#### 2. API路由层

##### 2.1 对话接口 (`backend/api/chat.py`)
```python
POST /api/chat
    - 接收用户消息，调用Agent处理
    - 支持WebSocket实时对话
    
POST /api/chat/reset
    - 重置对话状态
```

##### 2.2 行程接口 (`backend/api/itinerary.py`)
```python
POST /api/itinerary/generate
    - 生成行程
    
GET /api/itinerary/{itinerary_id}
    - 获取行程详情
    
PUT /api/itinerary/{itinerary_id}
    - 修改行程
```

##### 2.3 RAG接口 (`backend/api/rag.py`)
```python
POST /api/rag/query
    - RAG知识库查询
    
GET /api/rag/stats
    - 知识库统计信息
```

##### 2.4 工具接口 (`backend/api/tools.py`)
```python
GET /api/tools/weather/{city}
    - 天气查询
    
GET /api/tools/attractions
    - 景点查询
```

#### 3. 工具链

##### 3.1 景点查询工具 (`backend/tools/attraction.py`)
**功能：**
- 景点数据加载
- 按城市/偏好/评分筛选
- 景点评分排序
- 相似景点推荐

**关键类/方法：**
```python
class AttractionTool:
    def load_attractions() -> List[Attraction]
    def search_attractions(
        city: str,
        preferences: List[str],
        top_k: int
    ) -> List[Attraction]
    def filter_by_weather(attractions: List, weather: str) -> List
```

##### 3.2 路线规划工具 (`backend/tools/route.py`)
**功能：**
- 景点间距离计算（Haversine公式）
- 交通方式推荐
- 时间与费用估算
- 路线优化（贪心算法）

**关键类/方法：**
```python
class RouteTool:
    def calculate_distance(lat1, lng1, lat2, lng2) -> float
    def plan_route(from_attr, to_attr) -> RouteInfo
    def optimize_daily_route(attractions: List) -> List
```

##### 3.3 预算计算工具 (`backend/tools/budget.py`)
**功能：**
- 各类费用估算
- 总预算计算
- 预算超支调整

**关键类/方法：**
```python
class BudgetTool:
    def calculate_total_budget(itinerary: Itinerary) -> BudgetDetail
    def adjust_for_budget(itinerary: Itinerary, max_budget: int) -> Itinerary
```

#### 4. 行程规划器

##### 4.1 行程生成器 (`backend/planner/generator.py`)
**功能：**
- 景点筛选与评分
- 地理聚类（K-means）
- 按天分配景点
- 时间规划（游览+交通+餐饮）
- Markdown格式化输出

**关键类/方法：**
```python
class ItineraryGenerator:
    def generate(requirements: UserRequirements) -> Itinerary
    def select_attractions(preferences, budget) -> List
    def cluster_by_location(attractions, num_days) -> List[List]
    def plan_daily_schedule(attractions, weather) -> DailyPlan
    def format_markdown(itinerary) -> str
```

##### 4.2 行程优化器 (`backend/planner/optimizer.py`)
**功能：**
- 预算优化
- 路线优化
- 时间冲突检测

#### 5. 数据模型 (`backend/models/`)
**数据模型定义：**
- `user.py` - 用户模型
- `conversation.py` - 对话模型
- `itinerary.py` - 行程模型

#### 6. 数据库层 (`backend/db/`)
**功能：**
- SQLite数据库连接
- CRUD操作
- 历史记录存储

### 产出物清单

1. **代码模块：**
   - `backend/main.py` - FastAPI入口
   - `backend/api/*.py` - 所有API路由（4个文件）
   - `backend/tools/attraction.py` - 景点工具
   - `backend/tools/route.py` - 路线工具
   - `backend/tools/budget.py` - 预算工具
   - `backend/planner/generator.py` - 行程生成器
   - `backend/planner/optimizer.py` - 行程优化器
   - `backend/models/*.py` - 数据模型（3个文件）
   - `backend/db/*.py` - 数据库层

2. **配置文件：**
   - `backend/config/settings.py` - 配置管理
   - `backend/requirements.txt` - 依赖清单

3. **文档：**
   - API接口文档（Swagger自动生成）
   - 数据模型文档
   - 部署文档

4. **测试：**
   - API接口测试
   - 工具链单元测试
   - 行程生成算法测试

### 对接点

**→ 与成员A对接：**
- **接收调用：** 成员A的Agent会调用工具链
- **接口规范：**
  ```python
  # 成员A调用工具示例
  attractions = attraction_tool.search_attractions(
      city="杭州",
      preferences=["文化", "美食"],
      top_k=20
  )
  
  route = route_tool.plan_route(attr1, attr2)
  
  budget = budget_tool.calculate_total_budget(itinerary)
  ```

**→ 与成员B对接：**
- **读取数据：** 读取成员B提供的景点JSON文件
- **API封装：** 将成员B的RAG检索功能封装为API
- **天气工具：** 使用成员B开发的WeatherTool

**→ 与成员D对接：**
- **API提供者：** 为前端提供所有HTTP API
- **接口文档：** 维护Swagger文档，前端据此开发
- **数据格式：** 确保API返回格式符合前端需求
- **联调测试：** 与前端进行接口联调

**前后端接口规范示例：**
```python
# POST /api/chat
Request:
{
    "session_id": "uuid",
    "message": "我想去杭州玩3天"
}

Response:
{
    "success": true,
    "data": {
        "reply": "好的！请问您的预算大概是多少？",
        "stage": "collecting",
        "requirements": {
            "destination": "杭州",
            "days": 3,
            "budget": null,
            "preferences": []
        }
    }
}

# POST /api/itinerary/generate
Request:
{
    "session_id": "uuid",
    "requirements": {
        "destination": "杭州",
        "days": 3,
        "budget": 3000,
        "preferences": ["文化", "美食"]
    }
}

Response:
{
    "success": true,
    "data": {
        "itinerary_id": "uuid",
        "markdown": "# 杭州3日游...",
        "daily_plans": [
            {
                "day": 1,
                "date": "2025-11-20",
                "attractions": [...],
                "routes": [...],
                "budget": 800
            },
            ...
        ],
        "total_budget": 2800
    }
}
```

### 工作重点

1. **API设计**：RESTful规范，清晰的接口文档
2. **算法实现**：行程规划算法要合理高效
3. **性能优化**：API响应时间 < 3秒
4. **错误处理**：完善的异常处理和错误返回
5. **代码规范**：使用Pydantic做数据验证

---

## 🎨 成员D：前端工程师 + UI/UX

### 负责模块

#### 1. 项目搭建 (`frontend/`)
**功能：**
- Vue3 + Vite项目初始化
- Element Plus集成
- 路由配置（Vue Router）
- 状态管理配置（Pinia）
- Axios封装

#### 2. 页面开发

##### 2.1 对话页面 (`frontend/src/views/ChatView.vue`)
**功能：**
- 主页面布局（侧边栏 + 对话区）
- 对话流程控制
- 状态展示

**组件使用：**
- `<ChatBox>` - 对话框组件
- `<SideBar>` - 侧边栏组件
- `<MessageBubble>` - 消息气泡

##### 2.2 行程展示页面 (`frontend/src/views/ItineraryView.vue`)
**功能：**
- 行程详情展示
- 地图可视化
- 行程编辑入口

**组件使用：**
- `<ItineraryCard>` - 行程卡片
- `<MapView>` - 地图组件
- `<TimelineView>` - 时间轴

##### 2.3 历史记录页面 (`frontend/src/views/HistoryView.vue`)
**功能：**
- 历史对话列表
- 已生成行程列表
- 查看/删除操作

#### 3. 核心组件

##### 3.1 对话框组件 (`frontend/src/components/ChatBox.vue`)
**功能：**
- 消息列表滚动展示
- 消息气泡渲染
- 输入框
- 发送按钮
- 加载状态

**Props：**
```javascript
{
  messages: Array,      // 消息列表
  loading: Boolean,     // 加载状态
  placeholder: String   // 输入框占位符
}
```

**Events：**
```javascript
{
  'send-message': (message: string) => void,
  'reset': () => void
}
```

##### 3.2 消息气泡组件 (`frontend/src/components/MessageBubble.vue`)
**功能：**
- 用户/Agent消息样式区分
- Markdown渲染
- 时间戳显示
- 复制功能

##### 3.3 侧边栏组件 (`frontend/src/components/SideBar.vue`)
**功能：**
- 当前需求信息展示
- 快速示例按钮
- 功能导航

##### 3.4 行程卡片组件 (`frontend/src/components/ItineraryCard.vue`)
**功能：**
- 单日行程展示
- 景点列表
- 预算显示
- 展开/收起

##### 3.5 地图组件 (`frontend/src/components/MapView.vue`)
**功能：**
- 高德地图集成
- 景点标注
- 路线绘制
- 点击交互

#### 4. API封装 (`frontend/src/api/`)

##### 4.1 Axios配置 (`frontend/src/api/index.js`)
**功能：**
- Axios实例创建
- 请求/响应拦截器
- 错误统一处理
- BaseURL配置

##### 4.2 对话API (`frontend/src/api/chat.js`)
```javascript
export const chatAPI = {
  sendMessage(sessionId, message) {
    return axios.post('/api/chat', { session_id: sessionId, message })
  },
  resetChat(sessionId) {
    return axios.post('/api/chat/reset', { session_id: sessionId })
  }
}
```

##### 4.3 行程API (`frontend/src/api/itinerary.js`)
```javascript
export const itineraryAPI = {
  generate(requirements) {
    return axios.post('/api/itinerary/generate', requirements)
  },
  getDetail(itineraryId) {
    return axios.get(`/api/itinerary/${itineraryId}`)
  },
  update(itineraryId, data) {
    return axios.put(`/api/itinerary/${itineraryId}`, data)
  }
}
```

#### 5. 状态管理 (`frontend/src/stores/`)

##### 5.1 对话状态 (`frontend/src/stores/chat.js`)
```javascript
export const useChatStore = defineStore('chat', {
  state: () => ({
    sessionId: null,
    messages: [],
    currentRequirements: {},
    loading: false
  }),
  actions: {
    async sendMessage(message) { ... },
    addMessage(message) { ... },
    reset() { ... }
  }
})
```

##### 5.2 行程状态 (`frontend/src/stores/itinerary.js`)
```javascript
export const useItineraryStore = defineStore('itinerary', {
  state: () => ({
    currentItinerary: null,
    historyList: []
  }),
  actions: {
    async generateItinerary(requirements) { ... },
    async loadHistory() { ... }
  }
})
```

#### 6. 样式设计 (`frontend/src/assets/styles/`)
**功能：**
- 全局样式
- 主题变量
- 响应式断点
- 组件样式

### 产出物清单

1. **代码模块：**
   - `frontend/src/views/*.vue` - 3个页面
   - `frontend/src/components/*.vue` - 5-7个组件
   - `frontend/src/api/*.js` - API封装（3个文件）
   - `frontend/src/stores/*.js` - 状态管理（2个文件）
   - `frontend/src/router/index.js` - 路由配置
   - `frontend/src/assets/styles/` - 样式文件

2. **配置文件：**
   - `frontend/package.json` - 依赖配置
   - `frontend/vite.config.js` - Vite配置
   - `frontend/.env` - 环境变量

3. **文档：**
   - 前端组件文档
   - 页面交互说明
   - 样式规范文档

4. **测试：**
   - 组件单元测试（可选）
   - 页面功能测试

### 对接点

**→ 与成员C对接（核心对接）：**

**1. API接口对接**
- **接口文档：** 基于成员C提供的Swagger文档开发
- **联调流程：**
  1. 成员C提供接口文档和Mock数据
  2. 成员D基于文档封装API调用
  3. 后端开发完成后联调真实接口
  4. 测试各种场景和边界情况

**2. 数据格式对接**
```javascript
// 前端发送格式
{
  session_id: string,
  message: string
}

// 前端期望接收格式
{
  success: boolean,
  data: {
    reply: string,
    stage: string,
    requirements: object
  },
  error: string | null
}
```

**3. WebSocket对接（可选）**
- 实时对话需要WebSocket
- 消息格式约定
- 连接管理和重连机制

**→ 与成员A对接：**
- **需求确认：** 确认前端交互流程是否符合预期
- **测试：** 协助测试完整用户流程
- **UI Review：** 请成员A Review页面设计

**→ 与成员B对接：**
- 间接对接，通过API获取数据
- 了解RAG功能以便更好展示

### 工作重点

1. **用户体验**：交互流畅、加载反馈清晰
2. **响应式设计**：适配PC/平板/手机
3. **组件复用**：合理的组件拆分
4. **样式美观**：现代化、简洁的设计
5. **性能优化**：懒加载、虚拟滚动（长列表）

---

## 🔗 跨成员协作流程

### 1. 接口文档先行
**时间：** Day 1
**负责人：** 成员C
**协作：**
1. 成员C编写API接口文档（Swagger）
2. 成员A、成员D review接口设计
3. 确认无误后冻结接口（后续不轻易修改）

### 2. 并行开发
**时间：** Day 2-6
**协作：**
- 成员A、成员B、成员C：后端并行开发
- 成员D：前端基于接口文档 + Mock数据开发
- 每日同步进度，及时沟通问题

### 3. 联调测试
**时间：** Day 7-8
**协作：**
1. **后端内部联调**（成员A + 成员B + 成员C）
   - Agent调用RAG
   - Agent调用工具链
   - API端到端测试

2. **前后端联调**（成员C + 成员D）
   - 逐个API接口测试
   - 完整流程测试
   - 边界情况测试

### 4. 集成测试
**时间：** Day 9-10
**协作：** 全员
- 完整用户场景测试
- Bug修复
- 性能优化
- 演示准备

---

## 📋 关键交付物检查清单

### 成员A
- [ ] Agent核心逻辑完整可运行
- [ ] 对话流程自然流畅
- [ ] 信息抽取准确率 > 80%
- [ ] 提示词文档完善
- [ ] 单元测试通过

### 成员B
- [ ] 景点数据 ≥ 150条，质量合格
- [ ] 攻略文档 ≥ 50篇
- [ ] RAG检索功能正常，相关性好
- [ ] 向量数据库构建成功
- [ ] 天气API集成正常

### 成员C
- [ ] FastAPI服务可启动
- [ ] 所有API接口实现并测试通过
- [ ] 工具链功能正常
- [ ] 行程生成算法输出合理
- [ ] Swagger文档完整
- [ ] 响应时间 < 3秒

### 成员D
- [ ] 前端页面完整（3个主要页面）
- [ ] 所有组件正常工作
- [ ] 与后端API联调成功
- [ ] 响应式设计完成
- [ ] UI美观、交互流畅
- [ ] 无明显Bug

---

## 🎯 成功标准

### 核心功能（必须完成）
- ✅ 用户可以通过对话生成完整行程
- ✅ RAG知识库可查询并返回相关结果
- ✅ 前端展示美观，交互流畅
- ✅ API稳定，响应快速

### 技术亮点（课程评分点）
- ✅ **Agent架构**：完整的感知-决策-执行循环
- ✅ **RAG技术**：向量检索 + LLM生成
- ✅ **前后端分离**：现代Web开发模式
- ✅ **工程化**：代码规范、文档完善、测试覆盖

### 演示效果
- ✅ 能流畅演示完整流程（5-10分钟）
- ✅ 能展示RAG效果（知识库查询）
- ✅ 能展示个性化推荐（不同需求不同结果）
- ✅ UI美观专业

---

## 💬 沟通机制

### 日常沟通
- **微信群**：快速问题、进度同步
- **GitHub Issues**：Bug跟踪、功能讨论
- **腾讯会议**：关键节点会议

### 代码协作
- **Git分支策略**：功能分支开发，PR合并
- **Code Review**：相互Review，保证质量
- **命名规范**：统一的文件命名和代码风格

### 文档共享
- **飞书/腾讯文档**：协作文档
- **GitHub Wiki**：项目文档
- **Swagger**：API文档

---

**预祝项目成功！团队协作是关键！** 🚀
