# 景点数据集对比分析 - 成员B工作

## 📊 数据源对比

### 现有标准化格式 (zhejiang.json)

```json
{
  "id": "hz001",
  "name": "西湖",
  "city": "杭州",
  "province": "浙江省",
  "category": ["自然风光", "文化"],
  "rating": 4.8,
  "visit_duration": 3,
  "ticket_price": 0,
  "opening_hours": "全天",
  "best_season": ["春季", "秋季"],
  "description": "杭州西湖是中国著名的风景名胜区...",
  "location": {
    "lat": 30.2591,
    "lng": 120.1353,
    "address": "浙江省杭州市西湖区龙井路1号"
  },
  "tags": ["必游", "免费", "适合拍照", "世界遗产"],
  "tips": "建议清晨或傍晚游览，避开人流高峰..."
}
```

### Kaggle数据集格式 (qunar.csv)

```csv
名字, 链接, 地址, 介绍, 开放时间, 图片链接, 评分, 建议游玩时间, 建议季节, 门票, 小贴士, Page
```

---

## ✅ 已有字段 (Kaggle数据集提供)

| 标准字段 | Kaggle字段 | 状态 | 说明 |
|---------|-----------|------|------|
| `name` | `名字` | ✅ 完整 | 直接映射 |
| `rating` | `评分` | ✅ 完整 | 4.7 格式 |
| `description` | `介绍` | ✅ 完整 | 详细描述 |
| `opening_hours` | `开放时间` | ✅ 完整 | 需解析 |
| `ticket_price` | `门票` | ⚠️ 需处理 | 字典格式，需提取 |
| `visit_duration` | `建议游玩时间` | ⚠️ 需处理 | 文本需转数字 |
| `best_season` | `建议季节` | ⚠️ 需处理 | 长文本需提取 |
| `tips` | `小贴士` | ✅ 完整 | 详细建议 |

---

## ❌ 缺失字段 (需要补充)

### 1. **ID字段** ❌
- **标准字段**: `id` (例如: "hz001")
- **Kaggle提供**: 无
- **解决方案**: 
  ```python
  # 自动生成规则
  city_code = get_city_code(city)  # 杭州→hz, 南京→nj
  id = f"{city_code}{index:03d}"  # hz001, hz002...
  ```

### 2. **城市字段** ❌
- **标准字段**: `city` (例如: "杭州")
- **Kaggle提供**: 无（只在地址中包含）
- **解决方案**:
  ```python
  # 从地址中提取
  address = "阿坝藏族羌族自治州九寨沟县..."
  city = extract_city_from_address(address)
  # 或从Page字段推断（阿坝.csv → 阿坝）
  ```

### 3. **省份字段** ❌
- **标准字段**: `province` (例如: "浙江省")
- **Kaggle提供**: 无
- **解决方案**:
  ```python
  # 城市→省份映射表
  CITY_PROVINCE_MAP = {
      "杭州": "浙江省",
      "南京": "江苏省",
      "广州": "广东省",
      ...
  }
  province = CITY_PROVINCE_MAP.get(city)
  ```

### 4. **类别字段** ⚠️ 部分缺失
- **标准字段**: `category` (例如: ["自然风光", "文化"])
- **Kaggle提供**: 无直接字段
- **解决方案**:
  ```python
  # 方案1: 从名称推断
  if "寺" in name or "庙" in name:
      category.append("宗教")
  if "山" in name or "峰" in name:
      category.append("自然风光")
  
  # 方案2: 从描述中提取关键词
  keywords = ["自然", "文化", "宗教", "现代", "美食"]
  category = extract_category_from_description(description, keywords)
  
  # 方案3: 使用LLM分类
  category = llm_classify(name, description)
  ```

### 5. **地理坐标** ❌ 最关键
- **标准字段**: `location.lat` 和 `location.lng`
- **Kaggle提供**: 无
- **解决方案**:
  ```python
  # 方案1: 高德地图API地理编码
  address = "浙江省杭州市西湖区龙井路1号"
  lat, lng = amap_geocode(address)
  
  # 方案2: 百度地图API
  lat, lng = baidu_geocode(address)
  
  # 方案3: 使用OSM/Nominatim（免费但可能不准确）
  lat, lng = osm_geocode(address)
  ```

### 6. **标签字段** ⚠️ 需要生成
- **标准字段**: `tags` (例如: ["必游", "免费", "适合拍照"])
- **Kaggle提供**: 无
- **解决方案**:
  ```python
  tags = []
  
  # 从门票判断
  if ticket_price == 0:
      tags.append("免费")
  
  # 从评分判断
  if rating >= 4.7:
      tags.append("必游")
  
  # 从描述中提取
  if "拍照" in description or "摄影" in description:
      tags.append("适合拍照")
  
  if "世界遗产" in description or "文化遗产" in description:
      tags.append("世界遗产")
  ```

### 7. **图片字段** ⚠️ 有但需处理
- **标准字段**: 可选的 `image_url`
- **Kaggle提供**: `图片链接` ✅
- **解决方案**:
  ```python
  # 直接使用
  image_url = row['图片链接']
  
  # 或下载到本地
  local_path = download_image(image_url, f"images/{id}.jpg")
  ```

---

## 📋 字段完整性评分

| 字段类别 | 完整度 | 说明 |
|---------|--------|------|
| 基本信息 | 🟢 90% | name, description, tips完整 |
| 评分时间 | 🟢 85% | rating完整，时间需解析 |
| 价格门票 | 🟡 70% | 需从字典中提取 |
| 位置信息 | 🔴 20% | 缺少坐标，城市省份需提取 |
| 分类标签 | 🔴 10% | 需要生成 |
| 元数据 | 🔴 0% | ID需生成 |

**总体完整度**: 🟡 **55%**

---

## 🔧 数据转换Pipeline

### Step 1: 数据清洗
```python
def clean_data(row):
    """清洗原始数据"""
    # 1. 去除空白和特殊字符
    name = row['名字'].strip()
    
    # 2. 解析评分
    rating = float(row['评分']) if row['评分'] else None
    
    # 3. 解析门票价格
    ticket_dict = eval(row['门票'])  # 字典格式
    ticket_price = extract_min_price(ticket_dict)
    
    # 4. 解析游玩时间
    duration_text = row['建议游玩时间']  # "建议游览时间：3天"
    visit_duration = extract_duration_hours(duration_text)
    
    return cleaned_data
```

### Step 2: 字段补充
```python
def enrich_data(cleaned_data):
    """补充缺失字段"""
    # 1. 生成ID
    id = generate_attraction_id(cleaned_data['city'])
    
    # 2. 提取城市和省份
    city = extract_city(cleaned_data['地址'])
    province = CITY_PROVINCE_MAP[city]
    
    # 3. 地理编码 (关键步骤)
    lat, lng = amap_geocode(cleaned_data['地址'])
    
    # 4. 分类推断
    category = classify_attraction(
        cleaned_data['名字'],
        cleaned_data['介绍']
    )
    
    # 5. 生成标签
    tags = generate_tags(cleaned_data)
    
    # 6. 提取最佳季节
    best_season = extract_seasons(cleaned_data['建议季节'])
    
    return enriched_data
```

### Step 3: 格式转换
```python
def transform_to_standard(enriched_data):
    """转换为标准格式"""
    standard_format = {
        "id": enriched_data['id'],
        "name": enriched_data['name'],
        "city": enriched_data['city'],
        "province": enriched_data['province'],
        "category": enriched_data['category'],
        "rating": enriched_data['rating'],
        "visit_duration": enriched_data['visit_duration'],
        "ticket_price": enriched_data['ticket_price'],
        "opening_hours": enriched_data['opening_hours'],
        "best_season": enriched_data['best_season'],
        "description": enriched_data['description'],
        "location": {
            "lat": enriched_data['lat'],
            "lng": enriched_data['lng'],
            "address": enriched_data['address']
        },
        "tags": enriched_data['tags'],
        "tips": enriched_data['tips']
    }
    
    return standard_format
```

---

## ⚠️ 关键挑战和解决方案

### 挑战1: 地理坐标缺失 ⭐ 最大挑战
**影响**: 无法进行路线规划、距离计算、地图展示

**解决方案**:
1. **使用高德地图API** (推荐)
   ```python
   # 地理编码API
   url = "https://restapi.amap.com/v3/geocode/geo"
   params = {
       'key': AMAP_API_KEY,
       'address': address
   }
   response = requests.get(url, params=params)
   lat, lng = parse_location(response.json())
   ```
   - 优点: 准确率高，国内地址友好
   - 缺点: 需要API Key，有配额限制

2. **批量处理策略**
   ```python
   # 缓存已查询的坐标
   location_cache = {}
   
   for attraction in attractions:
       address = attraction['address']
       if address in location_cache:
           lat, lng = location_cache[address]
       else:
           lat, lng = geocode(address)
           location_cache[address] = (lat, lng)
           time.sleep(0.1)  # 避免频率限制
   ```

3. **降级方案**
   ```python
   # 如果API失败，使用城市中心坐标 + 偏移
   if lat is None:
       lat, lng = CITY_CENTER_COORDS[city]
       # 添加随机偏移避免重叠
       lat += random.uniform(-0.01, 0.01)
       lng += random.uniform(-0.01, 0.01)
   ```

### 挑战2: 类别分类不明确
**影响**: 用户偏好匹配不准确

**解决方案**:
```python
# 使用LLM进行智能分类
def classify_with_llm(name, description):
    prompt = f"""
    请将以下景点分类，从这些类别中选择1-3个最合适的：
    - 自然风光
    - 历史文化
    - 现代建筑
    - 美食
    - 购物
    - 宗教
    - 娱乐
    
    景点名称：{name}
    景点描述：{description[:200]}
    
    请以JSON数组格式返回，例如：["自然风光", "文化"]
    """
    
    category = llm.extract_json(prompt)
    return category
```

### 挑战3: 门票价格格式复杂
**Kaggle数据**: 
```python
{'九寨沟风景区门票+观光车票成人票\n\n': ['¥278起'], 
 '九寨沟风景区门票+观光车票学生票\n\n': ['¥185起']}
```

**解决方案**:
```python
def extract_ticket_price(ticket_dict):
    """提取最低成人票价"""
    if not ticket_dict:
        return 0
    
    # 查找成人票
    for key, value in ticket_dict.items():
        if '成人票' in key:
            price_str = value[0]  # '¥278起'
            price = int(re.search(r'(\d+)', price_str).group(1))
            return price
    
    # 如果没有成人票，返回第一个价格
    first_price = list(ticket_dict.values())[0][0]
    price = int(re.search(r'(\d+)', first_price).group(1))
    return price
```

### 挑战4: 游玩时间格式不统一
**Kaggle数据**: 
- "建议游览时间：3天"
- "建议游览时间：3天 - 6天"
- "建议游览时间：12小时 - 3天"

**解决方案**:
```python
def parse_visit_duration(duration_text):
    """解析游玩时间为小时数"""
    if not duration_text:
        return 3  # 默认3小时
    
    # 提取数字和单位
    if '天' in duration_text:
        days = int(re.search(r'(\d+)\s*天', duration_text).group(1))
        return days * 8  # 假设每天8小时
    elif '小时' in duration_text:
        hours = int(re.search(r'(\d+)\s*小时', duration_text).group(1))
        return hours
    else:
        return 3
```

---

## 📊 数据质量提升方案

### 优先级P0 (必须完成)
1. ✅ 基础字段提取 (name, rating, description)
2. ❌ 地理坐标补充 (使用高德API)
3. ❌ ID生成
4. ❌ 城市/省份提取

### 优先级P1 (重要)
5. ❌ 类别分类 (使用LLM或规则)
6. ❌ 门票价格解析
7. ❌ 游玩时间标准化

### 优先级P2 (可选)
8. ❌ 标签生成
9. ❌ 最佳季节提取
10. ❌ 图片下载

---

## 🎯 实施建议

### 方案A: 快速启动 (2-3天)
1. 使用Kaggle数据 + 手动补充关键字段
2. 先处理杭州、南京、苏州3个重点城市 (各30个景点)
3. 手动添加坐标 (使用高德地图拾取坐标工具)
4. 手动分类

**优点**: 快速上线，数据质量高
**缺点**: 扩展性差，人工成本高

### 方案B: 半自动化 (4-5天) ⭐ 推荐
1. 编写数据转换脚本
2. 集成高德地图API自动地理编码
3. 使用LLM进行自动分类
4. 人工审核关键字段

**优点**: 平衡效率和质量
**缺点**: 需要API Key和一些开发工作

### 方案C: 全自动化 (7-10天)
1. 完整的ETL Pipeline
2. 多个API集成
3. LLM增强
4. 自动化测试

**优点**: 可扩展到更多城市
**缺点**: 开发周期长

---

## 💡 下一步行动

### 立即行动 (今天)
1. ✅ 分析Kaggle数据集格式
2. ✅ 识别缺失字段
3. ⬜ 注册高德地图API Key
4. ⬜ 编写数据转换脚本框架

### 明天
5. ⬜ 实现地理编码功能
6. ⬜ 实现类别分类功能
7. ⬜ 处理杭州示例数据 (10个景点)

### 后天
8. ⬜ 批量处理浙江/江苏/广东数据
9. ⬜ 数据质量检查
10. ⬜ 导入向量数据库

---

## 📈 预期成果

完成数据转换后，将获得:
- ✅ 150+ 高质量景点数据
- ✅ 完整的地理坐标信息
- ✅ 准确的分类标签
- ✅ 标准化的JSON格式
- ✅ 可直接导入RAG系统

**数据质量目标**: 从55% → 95%+

